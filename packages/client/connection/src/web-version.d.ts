/**
 * Browser-visible product version shared by the host Web bundle and the
 * browser connection client. The Web bundle injects this value into the page
 * so the client can report the serving dsh release in product chrome (the
 * Settings General section's version row).
 */
/** Global name the Web bundle uses to expose the product version to the browser. */
export declare const WEB_VERSION_GLOBAL: '__DSH_WEB_VERSION__'
//# sourceMappingURL=web-version.d.ts.map
